<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="buttonRow">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
    </style>

    <style name="buttonNumber">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@null</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="buttonOperator" parent="buttonNumber">
        <item name="android:textColor">#4F46E5</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style cho popup menu trắng chữ đen -->
    <style name="PopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@android:color/white</item> <!-- Nền trắng -->
        <item name="android:textColor">#222222</item> <!-- Chữ đen (có thể dùng @android:color/black) -->
    </style>

    <!-- Overlay cho Toolbar -->
    <style name="ToolbarPopupMenuOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:popupMenuStyle">@style/PopupMenuStyle</item>
    </style>
</resources>
